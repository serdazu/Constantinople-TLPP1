import sqlite3


conexion=sqlite3.Connection(database="Q:\KONSTANTINOPLE\key.db")
cursor=conexion.execute("select ID,Usuario,Contraseña from CONTRASEÑAS")
for fila in cursor:
    print(fila)
    fila=(1, 'CONSTANTINOPLE', 'U58K3Y.2023')
    if fila:True,
    import Ventana

else: fila:False
print ("No se ha detectado el dispositivo KEY USB")

#selecciona e imprime los valores de la tabla de datos

conexion.close()